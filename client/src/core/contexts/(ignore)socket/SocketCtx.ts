// import { Socket } from "socket.io-client";
// import { createContext, useContext } from "react";

// interface SocketCtxType {
//   socket: Socket | null;
// }

// export const SocketCtx = createContext<SocketCtxType | null>(null);

// export const useSocket = (): SocketCtxType => {
//   const ctx = useContext(SocketCtx);

//   if (!ctx) throw new Error("context must be consumed within a provider 😠");

//   return ctx;
// };
